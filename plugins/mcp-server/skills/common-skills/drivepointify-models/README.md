How to convert a customer's own Excel budget / forecast / department template into a SmartModel tab — one date spine with history in the series, their logic rebuilt as Key Drivers / Key Results, a Budget Summary instead of Total/LY/variance columns, values tied to the source — and verify the uploaded model with the model tools. Read when a user uploads a non-Drivepoint workbook and asks to drivepointify it, make it Drivepoint compatible, or add key drivers and results.
